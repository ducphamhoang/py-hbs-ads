# Live Hermes Discord <-> Notion Scrum state is operator-local.
*

# Keep policy docs and sanitized templates versioned.
!.gitignore
!README.md
!*.example.json
!*.example.jsonl

# Bootstrap empty-container — committed as the canonical starting point for fresh installs.
!people_state.json
