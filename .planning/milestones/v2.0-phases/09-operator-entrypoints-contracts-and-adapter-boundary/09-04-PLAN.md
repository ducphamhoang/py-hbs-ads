---
phase: 09-operator-entrypoints-contracts-and-adapter-boundary
plan: 04
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - scripts/notion_scrum/preflight.py
  - tests/test_notion_scrum.py
autonomous: true
requirements:
  - ENTRY-03
  - CONTRACT-01

must_haves:
  truths:
    - "preflight.py validates registry integrity, prompt integrity, unresolved people, duplicate prompt IDs, and state consistency"
    - "preflight.py returns one stable result envelope"
    - "preflight.py does not mutate state"
  artifacts:
    - path: "scripts/notion_scrum/preflight.py"
      provides: "Level 3 operational preflight entrypoint"
      exports:
        - run_preflight
  key_links:
    - from: "scripts/notion_scrum/preflight.py"
      to: "scripts/notion_scrum/prompt_store.py"
      via: "uses validate_prompt_schema"
      pattern: "from prompt_store import load_prompts, validate_prompt_schema"
---

<objective>
Add the one-command operational preflight check for the Hermes Discord <-> Notion Scrum local state.
</objective>

<context>
Read first:
- `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-CONTEXT.md`
- `docs/agent/hermes-discord-notion-scrum-state-schema.md`
- `scripts/notion_scrum/scrum_state_doctor.py`
- `scripts/notion_scrum/common.py`
- `scripts/notion_scrum/prompt_store.py`
- `scripts/notion_scrum/result_contracts.py`
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement preflight.py</name>
  <files>scripts/notion_scrum/preflight.py, tests/test_notion_scrum.py</files>
  <behavior>
    - CLI flags: `--registry`, `--state`, `--audit-log`.
    - Function `run_preflight(registry_path=..., state_path=..., audit_log_path=...) -> dict`.
    - Validate `identity_index` entries point to existing `people`.
    - Report unresolved Notion mappings where active people have no Notion user/page mapping.
    - Detect duplicate `pending_prompt_id` values.
    - Validate every prompt with `validate_prompt_schema`.
    - For open prompts, require `source.thread_id`, a Notion task or project target, and `question.allowed_update_types`.
    - Emit a `PREFLIGHT_RUN` audit event summarizing warning/error counts.
    - Return a stable envelope with `ok=True` only when no errors are present; include warnings and errors under `data`.
  </behavior>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/test_notion_scrum.py -q</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- `preflight.py` reports registry, prompt, duplicate ID, unresolved mapping, and state consistency issues in one JSON result.
- Preflight does not mutate registry or prompt state.
- Existing and new tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-04-SUMMARY.md`.
</output>
