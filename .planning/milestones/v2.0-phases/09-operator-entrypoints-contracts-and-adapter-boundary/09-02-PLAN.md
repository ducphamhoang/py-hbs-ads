---
phase: 09-operator-entrypoints-contracts-and-adapter-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - scripts/notion_scrum/create_pending_prompt.py
  - tests/test_notion_scrum.py
autonomous: true
requirements:
  - ENTRY-02
  - CONTRACT-01
  - CONTRACT-02

must_haves:
  truths:
    - "create_pending_prompt.py records valid prompts in one command"
    - "create_pending_prompt.py rejects invalid prompts with a stable result envelope"
    - "create_pending_prompt.py writes audit only after a prompt is accepted"
  artifacts:
    - path: "scripts/notion_scrum/create_pending_prompt.py"
      provides: "Level 3 prompt creation entrypoint"
      exports:
        - create_prompt
  key_links:
    - from: "scripts/notion_scrum/create_pending_prompt.py"
      to: "scripts/notion_scrum/prompt_store.py"
      via: "uses validate_prompt_schema and append_prompt"
      pattern: "from prompt_store import append_prompt, validate_prompt_schema"
---

<objective>
Add the operator-facing prompt creation entrypoint with validation, audit, and a stable JSON envelope.
</objective>

<context>
Read first:
- `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-CONTEXT.md`
- `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-01-SUMMARY.md`
- `docs/agent/hermes-discord-notion-scrum-state-schema.md`
- `scripts/notion_scrum/record_pending_prompt.py`
- `scripts/notion_scrum/prompt_store.py`
- `scripts/notion_scrum/audit.py`
- `scripts/notion_scrum/result_contracts.py`
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement create_pending_prompt.py</name>
  <files>scripts/notion_scrum/create_pending_prompt.py, tests/test_notion_scrum.py</files>
  <behavior>
    - CLI accepts prompt JSON on stdin.
    - Flags: `--state`, `--audit-log`.
    - Function `create_prompt(prompt, state_path=..., audit_log_path=...) -> dict` validates with `validate_prompt_schema`.
    - On validation errors, return envelope with `ok=False`, `action_taken="validation_failed"`, `errors=[...]`, and no state mutation.
    - On success, call `append_prompt`, emit `PROMPT_RECORDED` audit event, and return envelope with `ok=True`, `action_taken="prompt_recorded"`, `pending_prompt_id`, and `audit_events=["prompt_recorded"]`.
    - Use `build_result` from `result_contracts.py` for the output shape.
  </behavior>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/test_notion_scrum.py -q</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- `create_pending_prompt.py` exits with stable JSON for valid and invalid prompts.
- Invalid prompts do not mutate prompt state.
- Valid prompts append to the prompt store and emit a prompt-recorded audit event.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-02-SUMMARY.md`.
</output>
