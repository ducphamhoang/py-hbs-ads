---
phase: 09-operator-entrypoints-contracts-and-adapter-boundary
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - scripts/notion_scrum/result_contracts.py
  - scripts/notion_scrum/notion_adapter.py
  - tests/test_notion_scrum.py
autonomous: true
requirements:
  - CONTRACT-01
  - CONTRACT-02
  - ADAPTER-01

must_haves:
  truths:
    - "All Level 3 entrypoints can build the same top-level JSON envelope shape"
    - "Notion-specific page/comment write behavior is callable through notion_adapter.py"
    - "Generic orchestration plans do not need to call raw Notion API helpers directly"
  artifacts:
    - path: "scripts/notion_scrum/result_contracts.py"
      provides: "Stable Level 3 result envelope helpers"
      exports:
        - build_result
        - merge_result
    - path: "scripts/notion_scrum/notion_adapter.py"
      provides: "Notion-specific adapter boundary over existing apply/update behavior"
      exports:
        - plan_actions
        - apply_actions
  key_links:
    - from: "scripts/notion_scrum/result_contracts.py"
      to: "scripts/notion_scrum/process_inbound_reply.py"
      via: "build_result used by all entrypoints"
      pattern: "from result_contracts import build_result"
    - from: "scripts/notion_scrum/notion_adapter.py"
      to: "scripts/notion_scrum/apply_notion_update.py"
      via: "adapter delegates to existing Notion action builder/executor"
      pattern: "from apply_notion_update import"
---

<objective>
Create the shared Level 3 result contract helper and the Notion adapter boundary before adding operator entrypoints.
</objective>

<context>
Read first:
- `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-CONTEXT.md`
- `docs/agent/hermes-discord-notion-scrum-prd-2026-04-20.md`
- `scripts/notion_scrum/apply_notion_update.py`
- `scripts/notion_scrum/audit.py`
- `tests/test_notion_scrum.py`
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add stable result envelope helper</name>
  <files>scripts/notion_scrum/result_contracts.py, tests/test_notion_scrum.py</files>
  <behavior>
    - Add `build_result(...) -> dict` that always returns the same top-level keys: `ok`, `action_taken`, `write_applied`, `requires_clarification`, `clarification_reason`, `pending_prompt_id`, `canonical_person_key`, `matched_prompt_id`, `resolved_update_type`, `audit_events`, `errors`, and `data`.
    - Defaults: `ok=False`, `action_taken="none"`, `write_applied=False`, `requires_clarification=False`, `clarification_reason=None`, IDs/types as `None`, `audit_events=[]`, `errors=[]`, `data={}`.
    - Add `merge_result(base, **overrides) -> dict` that preserves the complete key set while overriding supplied values.
    - Use stdlib only.
    - Add tests proving dry-run and execute-style envelopes have identical key sets.
  </behavior>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/test_notion_scrum.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Notion adapter boundary</name>
  <files>scripts/notion_scrum/notion_adapter.py, tests/test_notion_scrum.py</files>
  <behavior>
    - Add `plan_actions(plan, prompt, event, registry_path=DEFAULT_TEAM_REGISTRY) -> list[dict]` that delegates to `apply_notion_update.build_actions`.
    - Add `apply_actions(plan, prompt, event, execute, registry_path=..., state_path=..., audit_log_path=...) -> dict` that delegates to `apply_notion_update.apply_update`.
    - Keep Notion-specific action construction and write execution behind this adapter for Phase 09 entrypoints.
    - Add tests proving `plan_actions` returns the same action shape as the existing builder for a due-date proposal.
  </behavior>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/test_notion_scrum.py -q</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- `scripts/notion_scrum/result_contracts.py` exists and all result envelopes share identical top-level keys.
- `scripts/notion_scrum/notion_adapter.py` exists and isolates Notion action planning/apply calls for new entrypoints.
- Existing tests continue to pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operator-entrypoints-contracts-and-adapter-boundary/09-01-SUMMARY.md`.
</output>
