# Archived Legacy Backlog: Live SharePoint Authentication and Remote Transfer

**Archived:** 2026-04-22
**Former phase:** 999.1
**Status:** Legacy / not active

## Goal

Restore the real SharePoint operator loop from the legacy repo so the Python CLI can authenticate, search for footage remotely, download source assets into the workspace, and upload finished mixes back to SharePoint.

## Why Archived

This work was reviewed against `docs/agent/hermes-discord-notion-scrum-prd-2026-04-20.md` and found unrelated to the Hermes Discord <-> Notion Scrum PRD path.

The current active product direction is the Hermes shared-thread attributed automation workflow. The SharePoint media-transfer work is legacy Python rewrite backlog and should not affect GSD phase routing for the Hermes milestone.

## Original Requirements

- SHARE-02
- SHARE-03
- SHARE-04
- SHARE-05

## Original Success Criteria

1. The Python CLI can initiate and verify a real SharePoint login flow instead of only emulating a local library.
2. Operators can search/list remote SharePoint folders and files by partial name before downloading.
3. Operators can download exact files or search matches into the workspace using explicit result contracts and dry-run where practical.
4. Operators can upload rendered mix outputs back into predictable remote SharePoint variant folders.
5. Live-provider behavior is covered by adapter tests plus a documented manual rehearsal path for tenant-backed verification.

## Restore Procedure

If this becomes relevant again, recreate it as a fresh active milestone or promote it explicitly from this archive. Do not reintroduce it as `999.x` unless GSD backlog routing is updated to ignore archived backlog items.
